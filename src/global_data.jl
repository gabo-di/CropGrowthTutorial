days_before_sowing = Day(7) # start a week earlier to see water in soil
days_after_sowing = Year(1) + Month(6) # note that we need more than 1 year if we use GDD
minimal_pheno_data = 4 # the minimal amount of phenological data to use calibrate_phenology_parameters
